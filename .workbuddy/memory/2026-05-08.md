# 2026-05-08

## 启动后端
- 问题：pom.xml 中 maven-compiler-plugin 的 Lombok annotationProcessorPaths 配置被注释掉，导致 @Data 注解无法生效，编译报大量 getter/setter 找不到的错误
- 修复：恢复 maven-compiler-plugin 配置（去掉注释）
- 启动命令：java -jar target/e_learning-0.0.1-SNAPSHOT.jar（OSS 配置从 config 表中读取，无需环境变量）
- 端口：7788（context-path: /tzb），但本次实际启动端口显示为 61270（可能有端口覆盖）
- PID: 55224，日志：/tmp/e_learning_app.log
- 数据库连接正常（HikariPool 启动成功），OSS 配置从 DB config 表加载成功
